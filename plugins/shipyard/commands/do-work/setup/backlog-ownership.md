# /shipyard:do-work — Backlog bucket ownership

**The single source of truth for "who owns this issue?"** (issue [#1076](https://github.com/mattsears18/shipyard/issues/1076)). Every open issue in the backlog lands in exactly one bucket (first-match-wins, per [`01b-backlog-overview.md`](./01b-backlog-overview.md#2-backlog-overview)'s ordered list), and every bucket has exactly one owner: `/do-work`, `/my-turn`, another shipyard command, or an explicit **`nobody (by design)`** with a stated reason. `nobody (by design)` is a *decision* — writing the reason down is what tells it apart from a bucket that reaches nobody by accident.

## Normative source, and what this table is

[`04-backlog-divert.md`](./04-backlog-divert.md#4-fetch--rank-the-backlog)'s **client-side filter** is the normative definition of what `/do-work` actually dispatches — it's the code path that runs every session. [`01b-backlog-overview.md`](./01b-backlog-overview.md#2-backlog-overview)'s per-issue bucket table is a **rendering** of that filter for the upfront session summary. This file exists so the Owner column has exactly one home, instead of being re-described independently in each of `01b`, `04`, and `/my-turn` — the original defect #1076 fixes: three descriptions of the same routing, never cross-checked, silently drifted apart.

[`scripts/tests/backlog-ownership-coverage.test.sh`](../../../scripts/tests/backlog-ownership-coverage.test.sh) enforces two invariants mechanically, on every PR:

1. **Coverage.** Every bucket row below has a non-empty Owner, and every `nobody`-owned row has a stated reason in Notes. A bucket added here without an owner fails the suite.
2. **Structural agreement.** Every bucket `#` here appears in `01b`'s own bucket table (and vice versa) — a bucket one file knows about and the other doesn't is exactly the drift class #1076 found (the `agent-console` bucket was missing from `01b` entirely before this fix). Every label token cited here (`` `wontfix` ``, `` `agent-console` ``, ...) is asserted to be textually present in `04`'s client-side filter, so a label renamed or dropped from the dispatcher without updating this table fails loudly.

This is a **structural** check, not a full semantic-equivalence check — it does not compare `01b`'s prose description of a bucket's *disposition* against `04`'s actual runtime behavior word-for-word. Bucket 5 was the motivating example for that gap: `01b` and `04` used to describe the untriaged-issue bucket's handling in language that read differently even though both cited the same underlying behavior. [#1077](https://github.com/mattsears18/shipyard/issues/1077) reconciled the wording — `01b`'s bucket-5 row now states the trusted/untrusted split and the `triage.investigate_dispatch` config dependency directly, matching `04`'s client-side filter — so this table's Owner cell (`/do-work`, per the corrected tally in #1076) and `01b`'s prose no longer disagree.

## Ownership table

| # | Bucket | Criteria (label / condition) | `/do-work` disposition | Owner | Notes |
|---|---|---|---|---|---|
| 0.5 | Untrusted author | `author.login` NOT in `trusted_authors` | dropped from dispatch (unchanged); the drop ALSO applies `needs-human-review` + a bounded, idempotent handoff comment | **`/my-turn`** | Resolved by #1079 — the dispatch gate never changes; `04`'s client-side filter labels the dropped issue so it rides `/my-turn`'s existing bucket-5.5 path, bounded to issues newer than a persisted per-repo high-water mark (`$SHIPYARD_HOME/untrusted-author-highwater.json`) so an unauthenticated author can't force a full-history sweep. Idempotent via a `needs-human-review`-already-present check (no re-label, no re-comment on a repeat run). |
| 1 | Assigned to others | `assignees` contains a user other than `@me` — **only evaluated when `backlog.respect_assignees == true`** (config default `false`, issue #1248) | dropped — **config-precondition: `backlog.respect_assignees == true`** (default `false`; when `false` this bucket never matches and the issue falls through to bucket 8, Workable) | **`/my-turn`** | P2 housekeeping when the assignee has no activity for >30d (see [`my-turn.md`](../../my-turn.md) Pass B) — was a gap before #1076. On the default config (`respect_assignees: false`) this bucket is permanently empty — set `backlog.respect_assignees: true` (a genuine multi-contributor repo) to re-activate it. |
| 1.5 | Peer-claimed | issue/PR number in `.peer_sessions.claimed_targets` (set by [step 1.65](./01-repo-recovery.md#165-detect-live-peer-sessions-on-this-repo-1204)) | dropped this session — advisory, not a lock | **`/do-work`** | Self-clearing (#1204): excluded only while a live peer session's mtime is fresh AND still claims it; re-enters the workable queue once the peer's session file ages out (default 30min) or the peer finishes (issue closes / PR merges) |
| 2 | In flight | issue number in the `linked:pr` set | owned, tracked via its PR | **`/do-work`** | |
| 3 | Won't fix | carries `wontfix` | dropped | **nobody (by design)** | `wontfix` means no path forward — reaching nobody is the intended outcome |
| 4 | Discussion | carries `discussion` | dropped | **nobody (by design)** | A maintainer is already engaged in the thread by hand; not an oversight — was indistinguishable from one before #1076 |
| 5 | Needs triage | bot-shaped trusted author, OR symptom-shaped body (see [`04d`'s two signals](./04d-investigate-routing.md#the-two-signals-ord--either-one-is-sufficient), #1090) | trusted-author + any signal → routed to `investigate_candidates` and dispatched via investigate mode when `triage.investigate_dispatch` is `true` (default); untrusted-author → dropped (folds into 0.5) | **`/do-work`** (default) — **`/my-turn`** only when `triage.investigate_dispatch: false` | `investigate_dispatch: false` drops a matched issue entirely regardless of which signal matched, and is the *only* config under which this bucket is human-owned (#1077, generalized by #1090). `/my-turn`'s Pass B carries an untriaged-issue signal gated on that same config value — resolved once at setup (see [`my-turn.md`](../../my-turn.md) Setup step 3) — so it fires only in that narrow case and stays silent under the default. The `needs-triage` label was retired in #1120; this bucket is detection-only now, and `/my-turn` identifies it by re-running the same predicate rather than reading a label. See `04d`'s "Label retirement — completed" section. |
| 5.4 | Awaiting refinement | source-signal match (see `01b`) | processed by `/refine-issues` at session startup | **`/do-work`** (via `/refine-issues`) | |
| 5.5 | Awaiting human review | carries `needs-human-review` | dropped | **`/my-turn`** | The one originally-clean handoff. A bare `tracking` label is dropped separately, directly, by `04`'s filter (defensive gate, [#1081](https://github.com/mattsears18/shipyard/issues/1081)) — not yet in this bucket until step 3d.2 sub-sweep e migrates it to `needs-human-review` next session. **This `tracking` gate is PROVISIONAL, and as of [#1364](https://github.com/mattsears18/shipyard/issues/1364) it no longer fires on label presence alone** — `backlog-filter.sh classify` requires a content-sourced signal — the issue's **GitHub sub-issue graph** (the definitional case for the label, and the only *structured* signal; [#1556](https://github.com/mattsears18/shipyard/issues/1556)), a `Decision required` heading, an `Options` heading, a `Blocked by #N` reference, or a body task-list of issue references (#1556) — before dropping under the plain `tracking` reason (with an `evidence_pointer` citing the matched signal); absence of any signal still drops the issue but under the distinct `tracking-unjustified` reason, surfaced separately in [`04f-completion-ledger.md`](./04f-completion-ledger.md#the-bucket-taxonomy)'s census rather than folded silently into the generic human-gated count. |
| 5.6 | Agent-console (operator action) | carries `agent-console` (renamed from `needs-operator` #995; legacy-name recognition closed #1082) | dropped from code-worker dispatch | **`/do-work`** (operator phase) — **config-precondition: operator phase enabled (default; disabled by `--no-operate` / `--hands-off`)** | Owner assumes the operator phase is actually running — a per-invocation flag `/my-turn` cannot observe ([#1080](https://github.com/mattsears18/shipyard/issues/1080) tracks reading `/do-work`'s live state directly). Until then `/my-turn` reads the declared `my_turn.assume_operator_enabled` config knob (default `true`): `true` → surfaces one pointer line only, never walks it individually; `false` → the maintainer has declared these aren't being drained, so `/my-turn` walks the item directly ([#1093](https://github.com/mattsears18/shipyard/issues/1093)) |
| 6 | Blocked (soft label) | carries `blocked:agent-soft` | workable — NOT excluded | **`/do-work`** | Auto-clears at next session |
| 7 | Blocked (body reference / time-gate) | body matches `Blocked by #N` (#N still open), OR body carries a `do-work-blocked-until` marker still in the future ([#1161](https://github.com/mattsears18/shipyard/issues/1161)) | dropped | **`/do-work`** | Auto-clears the instant the blocker closes (body-ref case) or the date elapses (time-gate case) |
| 8 | Workable | everything else | dispatched | **`/do-work`** (implements) + **`/my-turn`** (surfaces a narrow "`/do-work` never claimed this" P2 housekeeping nudge) | Resolved by #1078 — the surfaced predicate now requires no `shipyard` label AND no self-assignment to `@me` (the two things `/do-work` stamps on every issue it dispatches) AND age past `my_turn.stale_undispatched_days` (default 7d), so it no longer matches the entire bucket — only issues `/do-work` genuinely never touched. |

## Reading this table

- **Bold Owner** is who acts on the issue next. `nobody (by design)` is a deliberate dead-end, not a gap — the Notes column always states why, and the coverage test rejects a `nobody` row with empty Notes.
- A row whose Notes cites a tracking issue is a **known, separately-scoped** gap or overlap at the time it was written, not something this table or its coverage check papers over. Resolving one of those issues means updating that row's Owner/Notes here, not re-deriving the table from scratch. **#1077 (bucket 5's `01b`-vs-`04` wording mismatch), #1079 (bucket 0.5's dropped-to-nobody handoff), and #1078 (bucket 8's whole-backlog overlap) were all this kind of gap and are now resolved** — the rows' Owner/Notes above reflect the reconciled routing / added handoff / narrowed signal.
- Issue [#1076](https://github.com/mattsears18/shipyard/issues/1076) is what gave buckets **1** and **4** their first real, stated owner. The three higher-value cells (0.5, 5, 8) were deliberately left as open, separately-tracked gaps rather than folded into that same PR — see the issue's own scope note. Bucket 5's wording gap is closed by #1077, bucket 0.5's dropped-to-nobody handoff is closed by #1079, and bucket 8's whole-backlog overlap is closed by #1078 — every gap #1076 opened is now resolved.
- **An Owner claim is only true under the config it was written against — say so on the row** ([#1093](https://github.com/mattsears18/shipyard/issues/1093)). "`/do-work` owns X" almost always means "`/do-work` owns X *when* `<knob>` is at its default value" — every `/do-work`-owned row whose disposition depends on a config knob (bucket 5's `triage.investigate_dispatch`, bucket 5.6's operator-phase-enabled precondition) states that precondition directly in the Owner cell, not just in prose buried in Notes, so a reader scanning the table alone doesn't mistake a config-conditional claim for an unconditional one. A partition that only holds under default config isn't a partition — the whole point of this table is to be checkable, and an unstated precondition is exactly the kind of drift #1076 exists to catch.
- **A rule this table's Notes marks provisional, defensive, or pending-migration may not fire silently on label presence alone** ([#1364](https://github.com/mattsears18/shipyard/issues/1364)). When a Notes cell describes a drop as provisional rather than a settled, intentional routing decision, the underlying filter (`backlog-filter.sh classify`) must ground that specific drop in a content-sourced justification — the issue's own body **or its structured GitHub state** ([#1556](https://github.com/mattsears18/shipyard/issues/1556): restricting the search to body prose is itself a way for a provisional gate to misfire, since the definitional evidence for a label may not be expressible as text) — and it must surface — under a distinct, greppable reason, visible in the end-of-session backlog census — the case where no justification can be found, rather than treating "label present" as sufficient on its own. Bucket 5.5's `tracking` row is the concrete instance: `reason: "tracking"` (with an `evidence_pointer` citing the matched body signal) for a justified drop, `reason: "tracking-unjustified"` for one that isn't. A settled gate (bucket 3's `wontfix`, bucket 4's `discussion`, bucket 5.5's own `needs-human-review`) has no such obligation — label presence alone is the entire, intended contract for those.
