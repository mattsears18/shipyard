# /shipyard:do-work — Setup phase

The session-startup steps (0.2 → 7). Runs once, end of phase hands off to [steady-state](./steady-state.md). The thin entry [`commands/do-work.md`](../do-work.md) owns the hot [orchestrator-state struct list](../do-work.md#orchestrator-state) and a pointer to the [session state file](../do-work.md#session-state-file) (cold long-tail detail split into [`orchestrator-state-reference.md`](./orchestrator-state-reference.md) and [`session-state-file.md`](./session-state-file.md)); this file owns the actual setup-step execution.

## This file is a thin router — load only the sub-phase you need

This phase used to live as one ~283KB file, which exceeds the 256KB single-file `Read` limit so the orchestrator couldn't read its own primary phase file in one call ([#611](https://github.com/mattsears18/shipyard/issues/611)). It's now split into step-cluster sub-files under [`setup/`](./setup/), mirroring the [thin-router + on-demand-load pattern](../do-work.md#phase-routing) the parent `do-work.md` already uses for its phase files. **Load only the sub-file(s) for the step(s) you're executing — not all eight.** The setup steps run in order (sub-phase 1 → 5), so a fresh session typically loads them sequentially as it advances; a re-entry that only needs one step (e.g. a deep-link from another phase file into the trusted-author allowlist) loads only that sub-file.

**The original #611 split was sized against the 256KB byte limit, not the per-`Read` 25,000-token cap that actually binds — three of the five sub-files still silently truncated on a plain `Read` ([#994](https://github.com/mattsears18/shipyard/issues/994)).** Those three (`00-config-worktree.md`, `01-repo-recovery.md`, `06-scope-preflight.md`) are each further split into 2–3 parts below, so **every** setup sub-file now fits a single `Read` call with margin. `04-backlog-divert.md` and `07-pool-fill.md` were already under the token cap and are unchanged.

| Setup sub-phase | Owns | When to load |
|---|---|---|
| [`setup/00-config-worktree.md`](./setup/00-config-worktree.md) | Run-once Setup preamble; steps **0.2 → 0.7 (intro)** (the step-**0.2** host-git health preflight that stops the session when the harness's own git is unusable ([#1567](https://github.com/mattsears18/shipyard/issues/1567)), `CLAUDE_PLUGIN_ROOT` re-export, repo-level opt-in check, the step-**0.41** staleness GATE that self-heals or refuses ([#1386](https://github.com/mattsears18/shipyard/issues/1386)), orchestrator-worktree relocation, per-worktree session-id storage, post-relocation re-read of pre-relocation spec files when stale, start of the fire-once parallelization contract) — the Lightweight C=1 index, and steps **0.42**, **0.45**, **0.55**, and **0.56** are stub pointers here; their full content lives in `00j`/`00h`/`00e`/`00f`/`00k` below | First — at session start, before any other setup step |
| [`setup/00b-parallelization-cache.md`](./setup/00b-parallelization-cache.md) | Rest of step **0.7** (canonical parallelization batch + background cleanup group) plus steps **0.8 → 0.9.1** (`blocker_state` cache, `gh-cached.sh` / `gh-batch.sh` wrappers) | Immediately after `00-config-worktree.md` |
| [`setup/00c-worktree-recovery.md`](./setup/00c-worktree-recovery.md) | Fragment of step **0.5** — recovery when `EnterWorktree` is offered but the call itself errors (e.g. a repo-level `WorktreeCreate` hook rejecting the payload): the create-then-enter-by-path recovery, the named-failure-source callout, and the worker-dispatch implication ([#1066](https://github.com/mattsears18/shipyard/issues/1066)) | Deep-link only, from `00`'s step-0.5 EnterWorktree-fallback pointer — not part of the ordered per-session walk |
| [`setup/00d-reread.md`](./setup/00d-reread.md) | Fragment of step **0.6** — re-read `dont.md`/`setup.md`/`00-config-worktree.md` fresh from the orchestrator worktree when step 0.4's staleness check fired, invalidating the pre-relocation copies already in context ([#1191](https://github.com/mattsears18/shipyard/issues/1191)); now a post-relocation backstop plus a missed-pre-relocation-step advisory, since step **0.42** performs the load-bearing re-read earlier, pre-relocation ([#1351](https://github.com/mattsears18/shipyard/issues/1351)) | Deep-link only, from `00`'s step-0.6 pointer — not part of the ordered per-session walk except when step 0.4 warned |
| [`setup/00e-pre-relocation-sweeps.md`](./setup/00e-pre-relocation-sweeps.md) | Step **0.45** — session-state init (steps 1 / 1.3 / 1.36 / 1.5, moved pre-relocation) plus the three worktree-cross-referencing sweeps (orphan orchestrator-worktree reap 1.6.5, stale agent-worktree reap 3b, orphan `do-work/*` branch triage 3c) that must run BEFORE `EnterWorktree` activates the worktree-isolation guard ([#1202](https://github.com/mattsears18/shipyard/issues/1202)) | Part of the ordered walk — from `00`'s step-0.45 pointer, after step 0.4 and before step 0.5 |
| [`setup/00f-session-id-storage.md`](./setup/00f-session-id-storage.md) | Step **0.55** — per-worktree `.shipyard-session-id` stash (not `/tmp`), the write-then-read-back mechanism, the newest-by-mtime derive helper ([#513](https://github.com/mattsears18/shipyard/issues/513)), and the `session-state.sh` cross-repo write guard ([#365](https://github.com/mattsears18/shipyard/issues/365)) | Part of the ordered walk — from `00`'s step-0.55 pointer, immediately after step 0.5 completes |
| [`setup/00g-redirect-target-refusal.md`](./setup/00g-redirect-target-refusal.md) | Fragment of step **0.5** — the third post-relocation Bash-guard refusal trigger: redirect-target validation ([#1325](https://github.com/mattsears18/shipyard/issues/1325)) — when a shell redirect's target sits outside the worktree (e.g. `/tmp/`), the guard refuses it and misattributes the error to git operations; remedy and documentation | Deep-link only, from `00`'s redirect-target refusal pointer — not part of the ordered per-session walk |
| [`setup/00i-staleness-gate.md`](./setup/00i-staleness-gate.md) | Step **0.41** — the staleness **GATE**: when step 0.4's check fires, [`heal-stale-primary-checkout.sh`](../../scripts/heal-stale-primary-checkout.sh) either fast-forwards a stale-but-clean primary checkout (then setup restarts from step 0.3 against the current spec) or **refuses and stops the session**. Replaces warn-and-continue, which let three shipped recovery fixes (0.42/0.45/0.6) go unreachable on exactly the checkouts that needed them, and let a stale `shipyard.config.json` silently revert every `models.*`/`trust.*`/`auto_merge.*` read to built-in defaults ([#1386](https://github.com/mattsears18/shipyard/issues/1386)) | Every session reaches the step-0.41 pointer (between step 0.4 and step 0.42), but the fragment's body is a no-op unless step 0.4 warned of staleness |
| [`setup/00h-immediate-reread.md`](./setup/00h-immediate-reread.md) | Step **0.42** — immediate, pre-relocation `git show`-based re-read of `dont.md`/`setup.md`/`00-config-worktree.md` when step 0.4's staleness check fires, plus diff-based detection of any newly-added pre-relocation-window (0.40–0.49) step heading the stale on-disk copy didn't have, so a step like #1202's 0.45 is caught and run instead of silently skipped ([#1351](https://github.com/mattsears18/shipyard/issues/1351)) | Every session reaches the step-0.42 pointer (between step 0.4 and step 0.45), but the fragment's body is a no-op unless step 0.4 warned of staleness |
| [`setup/00j-c1-path-index.md`](./setup/00j-c1-path-index.md) | Reference index — the Lightweight C=1 path: what's skipped and what stays at `--concurrency 1` (with a link to each gate's owning callout), how the inline-trivial fast path stacks orthogonally, and when to pick C=1 vs C≥2 ([#347](https://github.com/mattsears18/shipyard/issues/347); split out of `00-config-worktree.md`'s intro at its natural seam once that file crossed the token-budget cap, [#1431](https://github.com/mattsears18/shipyard/issues/1431)) | Deep-link only, from `00`'s intro pointer — a reference index, not a numbered step, so it is not part of the ordered per-session walk |
| [`setup/00k-repo-root-pin.md`](./setup/00k-repo-root-pin.md) | Step **0.56** — pin `SHIPYARD_REPO_ROOT` to the primary checkout so every post-relocation config read still sees the gitignored `.shipyard/config.local.json` layer ([#1059](https://github.com/mattsears18/shipyard/issues/1059); split out of `00-config-worktree.md` at its natural seam once that file crossed the token-budget cap, [#1431](https://github.com/mattsears18/shipyard/issues/1431)) | Part of the ordered walk — from `00`'s step-0.56 pointer, immediately after step 0.55 (`00f`) completes |
| [`setup/01-repo-recovery.md`](./setup/01-repo-recovery.md) | Steps **1 → 1.75** (resolve repo + user, silent-direct-merge repo-shape detection, missing-`workflow`-scope preflight warning, session-state init, orphan session-file + orphan orchestrator-worktree reaps, trusted-author allowlist, config-named-label existence verification) — step **1.6.5** is a stub pointer here (its full content lives in `01d` below); the GH App alias normalization sub-step of step 1.7 and all of step 1.75 are also stub pointers here (their full content lives in `01f`/`01e` below, [#1431](https://github.com/mattsears18/shipyard/issues/1431)) | After config + worktree relocation completes |
| [`setup/01b-backlog-overview.md`](./setup/01b-backlog-overview.md) | Step **2** (backlog overview — fetch, bucket, print the upfront status table) | Immediately after `01-repo-recovery.md` |
| [`setup/01c-label-recovery-refine.md`](./setup/01c-label-recovery-refine.md) | Steps **3 → 3.5** (label-ensure + prior-session recovery, refinement invocation) | Immediately after `01b-backlog-overview.md` |
| [Claude Code's worktree cleanup](https://code.claude.com/docs/en/worktrees#clean-up-subagent-and-background-session-worktrees) | Fragment of step **1.6.5** — full detail of the orphan orchestrator-worktree sweep (occupant-not-directory-name liveness fix [#1232](https://github.com/mattsears18/shipyard/issues/1232), reap command sequence, audit-log shape, skip condition, concurrency-safety analysis) [#1233](https://github.com/mattsears18/shipyard/issues/1233) | Deep-link only, from `01-repo-recovery.md`'s step-1.6.5 stub pointer (and from `00e-pre-relocation-sweeps.md`'s step-5 execution site) — not part of the ordered per-session walk |
| [`setup/01e-verify-config-labels.md`](./setup/01e-verify-config-labels.md) | Step **1.75** — verify every config-named label (`session_stamp`, `blocked_soft`, `ci_blocked`, `needs_human_review`, `user_feedback`, etc.) exists as a real label object in the target repo, via [`verify-config-labels.sh`](../../scripts/verify-config-labels.sh) ([#1359](https://github.com/mattsears18/shipyard/issues/1359); split out of `01-repo-recovery.md` at its natural seam once that file crossed the token-budget cap, [#1431](https://github.com/mattsears18/shipyard/issues/1431)) | Part of the ordered walk — from `01`'s step-1.75 pointer, immediately after step 1.7 (`01f`) completes, right before `01b`'s step 2 |
| [`setup/01f-gh-app-alias-normalization.md`](./setup/01f-gh-app-alias-normalization.md) | Fragment of step **1.7** — GH App login-shape alias normalization (`sentry[bot]` REST shape ↔ `app/sentry` GraphQL shape) via `trusted-authors-normalize.sh`, the CODEOWNERS protection recommendation for the override file, and the advisory-output line shapes ([#296](https://github.com/mattsears18/shipyard/issues/296); split out of `01-repo-recovery.md` at its natural seam once that file crossed the token-budget cap, [#1431](https://github.com/mattsears18/shipyard/issues/1431)) | Part of the ordered walk — from `01`'s step-1.7 pointer, immediately after the resolution-order logic resolves `trusted_authors` |
| [`setup/04-backlog-divert.md`](./setup/04-backlog-divert.md) | Steps **4 → 4.5** (fetch + rank the backlog, main-CI / PR-pileup divert checks) | After the backlog overview + label setup completes |
| [`setup/04j-failing-pr-snapshot.md`](./setup/04j-failing-pr-snapshot.md) | Steps **5 → 5.8** (failing-PR snapshot, seed inherited DIRTY PRs into `session_prs`, seed inherited draft PRs, flake-registry enforcement) — split out of `04-backlog-divert.md` at this seam when that file crossed the token-budget cap ([#1446](https://github.com/mattsears18/shipyard/issues/1446)) | Immediately after `04-backlog-divert.md` |
| [`setup/04b-untrusted-author-handoff.md`](./setup/04b-untrusted-author-handoff.md) | Fragment of step **4** — bucket-0.5 handoff (label + comment newly-dropped untrusted-author issues, bounded + idempotent, [#1079](https://github.com/mattsears18/shipyard/issues/1079)) | Deep-link only, from `04`'s trusted-author drop bullet — not part of the ordered per-session walk |
| [`setup/04c-draft-pr-recovery.md`](./setup/04c-draft-pr-recovery.md) | Step **5.75** — seed inherited draft PRs (classify + auto-ready the mechanically-safe ones, hand back the rest, [#1069](https://github.com/mattsears18/shipyard/issues/1069)) | Deep-link only, from `04j`'s step-5.75 pointer — not part of the ordered per-session walk |
| [`setup/04d-investigate-routing.md`](./setup/04d-investigate-routing.md) | Fragment of step **4** — investigate-mode candidate routing via live detection (bot author / symptom-shaped body; [#1090](https://github.com/mattsears18/shipyard/issues/1090), label retired [#1120](https://github.com/mattsears18/shipyard/issues/1120)) | Deep-link only, from `04`'s investigate-candidate routing bullet — not part of the ordered per-session walk |
| [`setup/04f-completion-ledger.md`](./setup/04f-completion-ledger.md) | Fragment of step **4** and of [`drain.md`'s termination assertion](./drain.md#termination-assertion) — the completion ledger every open issue must land in exactly one bucket of before either exit point may claim "backlog empty" / termination ([#1250](https://github.com/mattsears18/shipyard/issues/1250)) | Deep-link only, from `04`'s early-exit line and from `drain.md`'s termination assertion — not part of the ordered per-session walk |
| [`setup/04g-operator-routing.md`](./setup/04g-operator-routing.md) | Fragment of step **4** — `agent-console` routing to `operator_queue`, modeled explicitly as a route rather than a drop, mirroring the investigate-candidate route ([#1251](https://github.com/mattsears18/shipyard/issues/1251)) | Deep-link only, from `04`'s `agent-console` routing bullet — not part of the ordered per-session walk |
| [`setup/04h-post-main-ci-branch-refresh.md`](./setup/04h-post-main-ci-branch-refresh.md) | Fragment of step **4.5a** — the post-main-CI-fix branch-refresh that un-sticks session PRs carrying a stale failing required check ([#993](https://github.com/mattsears18/shipyard/issues/993)) | Deep-link only, from `04`'s `main_ci.status == "green"` transition bullet — not part of the ordered per-session walk |
| [`setup/04i-tracking-provisional-gate.md`](./setup/04i-tracking-provisional-gate.md) | Fragment of step **4** — `tracking`'s provisional-gate justification requirement (`has_tracking_justification()`'s precedence-ordered signal list — including the GitHub sub-issue graph, [#1556](https://github.com/mattsears18/shipyard/issues/1556) — and the `tracking` / `tracking-unjustified` verdict split, [#1364](https://github.com/mattsears18/shipyard/issues/1364)) | Deep-link only, from `04`'s `tracking` drop-label bullet — not part of the ordered per-session walk |
| [`setup/06-scope-preflight.md`](./setup/06-scope-preflight.md) | Start of step **6** (execution model, pre-scope synthetic-defer detectors, scope-result freshness check, PR-collision cache-reuse check pointer) | After the backlog fetch + divert checks complete |
| [`setup/06g-scope-staleness-probe.md`](./setup/06g-scope-staleness-probe.md) | Fragment of step **6** — the staleness probe (has this issue already landed on `origin/<default-branch>`?) and the scope agent's Ready/Deferred/Already-landed return-shape contracts — split out of `06-scope-preflight.md` at this seam when that file crossed the token-budget cap ([#1446](https://github.com/mattsears18/shipyard/issues/1446)) | Immediately after `06-scope-preflight.md` |
| [`setup/06b-scope-carveouts.md`](./setup/06b-scope-carveouts.md) | Rest of step **6** (design/arch/epic/spike in-scope-by-default rule, operator-slice carve-out, QA-verification carve-out, per-class `evidence_pointer` shape table) | Immediately after `06g-scope-staleness-probe.md` |
| [`setup/06c-scope-handling-ui.md`](./setup/06c-scope-handling-ui.md) | Rest of step 6 (per-returned-entry handling) | Immediately after `06b-scope-carveouts.md` |
| [`setup/06e-scope-ui-timing.md`](./setup/06e-scope-ui-timing.md) | Steps **6.5 → 6.8** (status-line + state-change-banner UI, setup-timing flush) — split out of `06c` at its natural seam when that file crossed the token-budget cap ([#1431](https://github.com/mattsears18/shipyard/issues/1431)) | Immediately after `06c-scope-handling-ui.md` |
| [`setup/06d-recheck-probe-authorship.md`](./setup/06d-recheck-probe-authorship.md) | Fragment of step **6**'s deferred-entries handling — step 4c, automated `do-work-recheck` probe-marker authorship for `external-dependency` defers whose `evidence_pointer` carries a recognized `Recheck probe:` hint ([#1201](https://github.com/mattsears18/shipyard/issues/1201)) | Deep-link only, from `06c`'s step-4c pointer — not part of the ordered per-session walk |
| [`setup/06f-pr-collision-cache-reuse.md`](./setup/06f-pr-collision-cache-reuse.md) | Fragment of step **6** — the `blocked-by-in-flight-pr` cache-reuse optimization: the `<!-- do-work-blocked-by-prs-scope -->` comment persistence, the PR-collision cache-reuse check's full procedure, and the mandatory semantic-premise re-validation against the merged PR's diff before ever skipping a fresh scope-agent dispatch ([#1448](https://github.com/mattsears18/shipyard/issues/1448)) | Deep-link only, from `06`'s PR-collision cache-reuse check pointer and from `drain.md` 5.b's `blocked-by-in-flight-pr` backstop — not part of the ordered per-session walk |
| [`setup/07-pool-fill.md`](./setup/07-pool-fill.md) | Step **7** (initial pool fill — dispatch the first wave of workers, then hand off to [steady-state](./steady-state.md)) | Last — after scope pre-flight, immediately before steady-state takes over |
| [`setup/backlog-ownership.md`](./setup/backlog-ownership.md) | **Reference, not a step** — the canonical bucket→owner table `01b-backlog-overview.md`'s UI rendering and `04-backlog-divert.md`'s dispatch filter are both checked against ([#1076](https://github.com/mattsears18/shipyard/issues/1076)); also referenced by [`/my-turn`](../my-turn.md) for its human-only subset | On demand — auditing or updating who owns a backlog bucket, not part of the ordered per-session walk |

### How to load on demand

Read only the sub-phase relevant to the step you're executing. A cold session walks them in order (00 → 00e → 00f → 00k → 00b → 01 → 01f → 01e → 01b → 01c → 04 → 04j → 06 → 06g → 06b → 06c → 06e → 07) as it advances through setup — `00e`, `00f`, and `00k` are loaded mid-way through `00`'s own step sequence (at step 0.45's, step 0.55's, and step 0.56's stub pointers respectively), and `01f`/`01e` are loaded mid-way through `01`'s own step sequence (at step 1.7's and step 1.75's stub pointers respectively), not after `00`/`01` finish; don't pre-load all eighteen at once. A **deep-link from another phase file** (e.g. [`steady-state.md`](./steady-state.md) referencing the [trusted-author allowlist](./setup/01-repo-recovery.md#17-resolve-trusted-author-allowlist), or [`drain.md`](./drain.md) referencing [seed-inherited-DIRTY-PRs](./setup/04j-failing-pr-snapshot.md#57-seed-inherited-dirty-prs-into-session_prs-cross-session-drain-hand-off)) targets exactly one sub-file — load that one, not the whole setup phase. The [`dont.md`](./dont.md) prohibition list stays a sidebar across every setup sub-phase, exactly as it is across the other phases.

**Don't pre-load adjacent sub-phases.** Each cluster is self-contained for its step range; pulling the others into context defeats the split that exists to keep every phase file under the read limit. The next step's sub-file loads when you reach it.
